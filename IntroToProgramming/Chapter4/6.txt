    def equal_to_four(x)
      if x == 4
        puts "yup"
      else
        puts "nope"
    end

    equal_to_four(5)

#####################################################
# Code above is missing `end` for if else loop
# We can fix it by adding `end` after else puts "nope"
#
#    def equal_to_four(x)
#      if x == 4
#        puts "yup"
#      else
#        puts "nope"
#      end
#    end
#
#    equal_to_four(5)
